# ============================================================
# requirements.txt - 项目依赖列表
# ============================================================
# 说明：
# 1. LLaMA-Factory 会自动安装兼容版本的 transformers, peft, trl
# 2. vLLM 需要单独安装
# 3. 其他依赖按需安装
# ============================================================

# 训练框架（通过 setup_env.sh 安装 LLaMA-Factory）
# 手动安装：pip install llamafactory

# 推理引擎
vllm>=0.6.0

# 数据处理
pandas>=2.0.0
tqdm>=4.66.0

# 评测指标
scikit-learn>=1.3.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 代码质量
ruff>=0.4.0
pytest>=8.0.0

# API 客户端
openai>=1.0.0
aiohttp>=3.9.0
