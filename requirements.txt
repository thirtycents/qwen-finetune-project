# ============================================================
# requirements.txt - 项目依赖列表
# ============================================================
# 安装方式：pip install -r requirements.txt
#
# 注意：先安装 LLaMA-Factory，它会自动安装兼容的依赖版本
# ============================================================

# ---- 训练框架（核心）----
# LLaMA-Factory 会自动安装兼容版本的 transformers, peft, trl 等
llamafactory

# ---- 推理服务 ----
# vLLM：高性能大模型推理引擎（PagedAttention）
vllm>=0.6.0

# ---- 数据处理 ----
pandas>=2.0.0
tqdm>=4.66.0

# ---- 评测 ----
scikit-learn>=1.3.0

# ---- 可视化 ----
matplotlib>=3.7.0
seaborn>=0.12.0

# ---- 代码质量 ----
ruff>=0.4.0
pytest>=8.0.0

# ---- API 客户端 ----
openai>=1.0.0
aiohttp>=3.9.0
